export function HeroSection() {
  return (
    <section id="hero" className="section-padding min-h-[70vh] flex flex-col items-center justify-center">
      <div className="centered-container">
        <p className="section-kicker">Save the date</p>
        <h1 className="section-title-script font-script">
          Placeholder nombres
        </h1>
        <p className="mt-4 font-serif text-lg text-[var(--text-muted)]">
          Fecha y lugar — placeholder
        </p>
      </div>
    </section>
  );
}
